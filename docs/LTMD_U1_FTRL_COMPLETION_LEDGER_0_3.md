# LTMD-U1 — ledger exhaustivo de completitud FTRL 0.3

La versión 0.3 promueve **W3 Español/Lengua** únicamente después de dos cierres independientes: validación computacional exhaustiva y preservación archivística privada. El denominador documental permanece inalterado en **542 identidades**.

## Cambio respecto de 0.2

W3 pasa de `pending` a `validated` para sus 130 identidades, con 114 objetos canónicos y 20,765 páginas. La corrida distribuida `32853375619` demostró una unión exacta y única de 52 shards; la consolidación privada reconstruyó el corpus y validó SQLite/FTS5/QC. Los productos restringidos, handoffs cifrados, evidencia text-free y acta privada de cierre fueron preservados en Google Drive no compartido.

La promoción **no** modifica los límites epistemológicos: `ocr_available != text_verified` y `corpus_ready != semantic_ready`. W3 conserva `text_verified=0` y `semantic_ready=0`.

## Estado exhaustivo tras W3

- Universo fijo: 542 identidades.
- `required_ftrl_processing`: 524.
- `active_retention`: 13.
- `final_exception`: 5.
- FTRL validado y archivísticamente cerrado: 188 identidades (W1 + W3 + W5).
- Disposición terminal estricta: 193/542 identidades (188 validadas + 5 excepciones finales), **35.6%**.
- Restan 349/542 identidades, **64.4%**: 336 procesables pendientes y 13 retenciones activas.

El cierre global continúa siendo inelegible mientras existan olas FTRL pendientes o retenciones activas.
