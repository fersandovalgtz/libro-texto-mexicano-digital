name: analyze-cn46-exact-page-overlap

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/analyze-cn46-exact-page-overlap.yml'
      - 'scripts/analyze_cn46_exact_page_overlap.py'
      - 'data/expansion/cn46_page_manifest.csv'

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Analyze exact page overlap
        run: python3 scripts/analyze_cn46_exact_page_overlap.py
      - name: Publish overlap analysis race-safely
        run: |
          set -euo pipefail
          cp data/expansion/cn46_exact_page_overlap.csv /tmp/cn46_exact_page_overlap.csv
          cp data/expansion/cn46_exact_page_overlap.md /tmp/cn46_exact_page_overlap.md
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            mkdir -p data/expansion
            cp /tmp/cn46_exact_page_overlap.csv data/expansion/
            cp /tmp/cn46_exact_page_overlap.md data/expansion/
            git add data/expansion/cn46_exact_page_overlap.csv data/expansion/cn46_exact_page_overlap.md
            if git diff --cached --quiet; then exit 0; fi
            git commit -m 'Publish CN4/CN6 exact page overlap analysis'
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
